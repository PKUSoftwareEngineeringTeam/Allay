interface compiler {
    enum file-type {
        markdown,
        html,
    }

    before-compile: func(source: string, ty: file-type) -> string;
    after-compile: func(compiled: string, ty: file-type) -> string;
}
