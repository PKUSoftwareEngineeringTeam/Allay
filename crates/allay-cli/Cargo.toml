[package]
name = "allay-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
allay-base = { path = "../allay-base" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-tree = "0.4.0"
